/*
    Here are definitions of needed peripherals.
    This file connects variables from Cube's C code with application's C++ code.
    Add any needed information about used peripherals in comments so that it is
    easy to recreate/modify them in Cube.
*/

#include "stm_config.h"


// *** PERIPHERALS ************************************************************
// Only assign variables or write VERY short functions
namespace stm {


/* EXAMPLE:
    // Delay Timer:
    // TIM10 (APB2), set prescaler to get f = 1MHz, later only change period
    extern "C" TIM_HandleTypeDef htim10;
    TIM_HandleTypeDef &delayTimer = htim10;
*/


} // namespace stm
